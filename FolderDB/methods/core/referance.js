// a way to establish references between files, documents
// maybe save it as a value _REF:pointer.to.referenced.file
// can we reference only files? Maybe yea, otherwise its too complex
async function ref(key, value) {}

// a way to get those references
async function populate(key, value) {}
